{
  "frontend/src/index.css": "Global styles with a warm teal/forest green OKLCH color system, Inter and Playfair Display font imports, and fade-in animation for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom font families (Inter, Playfair Display), card shadow utilities, additional border-radius tokens, and fade-in keyframe animation.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching all members, adding a single member (with membershipNumber as a required string parameter passed to the backend), and bulk-importing members.",
  "frontend/src/components/Layout.tsx": "App shell with sticky teal header, brand logo, navigation links for Register/Members/Bulk Upload pages, and a footer with dynamic year and caffeine.ai attribution.",
  "frontend/src/components/MembershipForm.tsx": "Controlled registration form with Membership Number as a required field at the top (marked with asterisk, validated on submit, shows error if blank), followed by name, spouse name, mobile, alternate mobile, address, area, and optional family member count.",
  "frontend/src/components/MemberCard.tsx": "Card component displaying membership number (id) at the top with a hash icon, then name and area badge, followed by phone, alternate phone, spouse name, address, area, and optional family member count at the bottom — no photo or avatar.",
  "frontend/src/components/MemberList.tsx": "Fetches all members via React Query and renders a responsive grid of MemberCards with skeleton loading, error state with retry, and empty state messaging; the `onMembersLoaded` callback prop has been removed since the parent now fetches data directly.",
  "frontend/src/pages/RegisterPage.tsx": "Registration page with a banner image overlay, page title, and the MembershipForm component inside a styled card container.",
  "frontend/src/pages/MembersPage.tsx": "Members listing page that directly uses the `useGetAllMembers` hook to power both the member grid and a \"Download Database\" button in the header; the button is only disabled during initial loading (not when the list is empty), and the CSV export correctly uses `membershipNumber` for the Membership Number column.",
  "frontend/src/App.tsx": "TanStack Router setup with root layout route, `/register`, `/members`, `/bulk-upload` routes, and an index redirect to `/register`.",
  "frontend/src/pages/BulkUploadPage.tsx": "Bulk CSV upload page with drag-and-drop, client-side CSV parsing treating Membership Number as a required column, preview table with row-level error messages for missing Membership Number, downloadable template, and submit button that only passes valid rows to useBulkAddMembers."
}